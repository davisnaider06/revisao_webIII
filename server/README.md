# Shopping List - Server

Servidor Express com Mongoose.

Instalação:

```
cd server
npm install
```

Executar (variantes):

```
npm run dev   # com nodemon
npm start     # inicia com node
```

Configure `MONGO_URI` em `.env` se necessário.
